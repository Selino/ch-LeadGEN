import { Meta } from "@storybook/addon-docs/blocks"
import ColorsImage from "../../images/color palette.svg"
import TypeImage from "../../images/fonts.png"

<Meta title='Platform/Tokens' />

# Tokens

Design tokens are the visual design atoms of the design system — specifically, they are named entities that store visual design attributes. We use them in place of hard-coded values (such as hex values for color or pixel values for spacing) in order to maintain a scalable and consistent visual system for UI development.

## Colors

Generic colors to use within an application. Tokens that have the prefix of "BRAND" are brandable tokens and are subject to change when a customer applies theming to their org.

<img src={ColorsImage} />

<p></p>

## Type

<img src={TypeImage} />
